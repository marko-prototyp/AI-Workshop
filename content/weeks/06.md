---
num: "06"
title: "Visual design system as code"
phase: "Design"
capability: "Design tokens; component thinking; translating Figma styles to CSS variables"
demo: "Translate Figma styles into a tokens.css file, then build a 'style tile' artifact that uses them. Show how editing one CSS variable propagates everywhere."
exercise:
  - "Formalize the visual system as CSS tokens"
  - "Build a style tile rendering all tokens"
  - "Generate any custom assets needed"
prompts:
  - title: "Tokens from Figma styles"
    body: |
      Here are my Figma styles:
      Colors: [list with names and hex]
      Type: [list with sizes/line heights]
      Spacing: [scale values]

      Convert into a clean tokens.css using CSS custom properties. Use semantic names where it makes sense. Comment the file so a designer reading it understands the system.
  - title: "Style tile"
    body: |
      Build me a style tile as an HTML artifact using my tokens.css [paste]. Show: color palette as labeled swatches, type scale with examples, spacing scale visualized, button states (default/hover/active/disabled) in MY style not default Tailwind, one example card and one form input. Visual direction "[name]". Banned moves: [paste].
outputs:
  - "tokens.css file"
  - "A style tile artifact"
  - "Custom assets if needed"
watch:
  - "Six colors max; people over-spec"
  - "Type scales should be ratio-based"
  - "CSS variables are the unlock for week 7+"
---
